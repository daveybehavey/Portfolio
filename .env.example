# Optional: Google Analytics 4 — Admin → Data streams → Web → Measurement ID (G-XXXXXXXX)
# For GitHub Actions deploys, also add repo secret NEXT_PUBLIC_GA_MEASUREMENT_ID
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Optional: Cloudflare Web Analytics (privacy-friendly, no cookie banner required for basic use)
# Dashboard → Analytics & logs → Web Analytics → Add site → copy beacon token
NEXT_PUBLIC_CF_WEB_ANALYTICS_TOKEN=

# Optional: scripts/cf-api.js (DNS, email routing helpers — not needed for local dev)
# CLOUDFLARE_API_TOKEN=
# CLOUDFLARE_API_EMAIL=
# CLOUDFLARE_GLOBAL_API_KEY=
